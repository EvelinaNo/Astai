import { StyledButton } from "./StyledNavbar";

export const Navbar = () => {

    return (
        <div>
    <nav style={{ backgroundColor: "pink", padding: "50px 30px" }}>Navbar</nav>;
    <StyledButton>Styled-components Button</StyledButton>
    </div>
    );
}